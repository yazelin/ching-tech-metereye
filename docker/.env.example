# PostgreSQL Database Configuration
# Copy this file to .env and modify the values

# Database port (external)
DB_PORT=5433

# App user (used by MeterEye application)
DB_USER=metereye
DB_PASSWORD=change_me_in_production
DB_NAME=metereye

# Read-only user (for external tools like Grafana, Metabase, etc.)
DB_READONLY_USER=metereye_reader
DB_READONLY_PASSWORD=reader_password_change_me

# Connection string for MeterEye config.yaml:
# postgresql+psycopg://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}
#
# Example:
# postgresql+psycopg://metereye:change_me_in_production@localhost:5433/metereye
#
# Read-only connection (for external tools):
# postgresql://${DB_READONLY_USER}:${DB_READONLY_PASSWORD}@<host>:${DB_PORT}/${DB_NAME}
